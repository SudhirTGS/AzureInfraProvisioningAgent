terraform {
  # Local state for now — fine for solo/test use. Once this moves to a
  # shared/team setup, switch back to the "azurerm" backend (resource_group_name,
  # storage_account_name, container_name, key = "{{ tf_state_key }}") pointing at a
  # real remote state storage account.
  backend "local" {}
}
